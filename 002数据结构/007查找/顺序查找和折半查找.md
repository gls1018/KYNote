

## 1. 相关概念

**平均查找长度ASL (Averial Search Length)**

在查找过程中, 一次查找的长度是指需要比较关键字的次数, 平均查找长度是所有查找过程中进行关键字比较次数的平均值.
$$
ASL = \sum_{i=1}^{n}P_iC_i
$$

- n是查找表的长度
- P~i~是查找第i个元素的概率
- C~i~是找到第i个元素所需要进行比较的次数



## 2. 顺序查找



顺序查找适用于链表和顺序表

- 对于顺序表, 直接对数组下标进行迭代遍历元素
- 对于链表,通过next指针进行遍历



### 2.1 一般线性表的顺序查找



```cpp
typdef struct
{
    ElemType *elem;  //元素基址
    int TableLen;    //表的长度
}SSTable;

int Search_seq(SSTable ST, ElemType key)
{
    for(int i=0; i<ST.TableLen && ST.eem[i] != key; i++)
    {
        return i==ST.TableLen ? -1 : i;
    }
}
```



- 已知每个元素被查找的概率相等. n个元素, 每个元素的概率是$\dfrac{1}{n}$
- 定位表中第i个元素时, 需要比较 i次

 $ASL_{成功} = \sum_{i=1}^{n}\dfrac{1}{n}i = \dfrac{n+1}{2}$



优缺点分析:

- 缺点是当 n 很大时, 查找效率低
- 优点是对数据的存储没有要求, 顺序存储和链式存储皆可.
- 对表中记录的有序性也没有要求.

### 2.2 有序线性表的顺序查找







## 3. 折半查找

折半查找仅仅适用于有序的顺序表

- 有序
- 顺序表, 不能是链表.



```cpp
```







## 4. 分块查找

